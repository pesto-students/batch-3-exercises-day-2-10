let root = () => {
  return Math.sqrt(49);
}

let pow = () =>{
  return Math.pow(4,2)
}

let rnd = () =>{
  return Math.round(7.14);
}

let sqrt = root();
let power = pow();
let round  = rnd();

export {
  sqrt,
  power,
  round

};
