let squareRoot = () => {
    return Math.sqrt(49);
};

let powerToTwo = () => {
    return Math.pow(4, 2);
};

let roundOff = () => {
    return Math.round(7.2);
};

let sqrt = squareRoot();
let power = powerToTwo();
let round = roundOff();

export { sqrt, power, round };
